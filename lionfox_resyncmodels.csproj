<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <AssemblyName>lionfox_resyncmodels</AssemblyName>
    <RootNamespace>lionfox_resyncmodels</RootNamespace>
    <OutputPath>../lionfox_resyncmodels</OutputPath>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <CopyLocalLockFileAssemblies>false</CopyLocalLockFileAssemblies>
  </PropertyGroup>

  <!-- ── Adjust VS_PATH to your Vintage Story install directory ── -->
  <PropertyGroup>
    <VS_PATH Condition="'$(VS_PATH)' == ''">D:\Vintagestory</VS_PATH>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="VintagestoryAPI">
      <HintPath>$(VS_PATH)\VintagestoryAPI.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="VintagestoryServer">
      <HintPath>$(VS_PATH)\VintagestoryServer.dll</HintPath>
      <Private>false</Private>
    </Reference>
  </ItemGroup>

</Project>
